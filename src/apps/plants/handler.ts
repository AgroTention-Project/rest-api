import type { Handler, Request, Response } from "express";

export const listPlantsHandler: Handler = async (
  req: Request,
  res: Response
): Promise<void> => {};

export const getPlantHandler: Handler = async (
  req: Request,
  res: Response
): Promise<void> => {};
